// Game dimensions
export const CANVAS_WIDTH = 400;
export const CANVAS_HEIGHT = 600;

// Physics constants
export const GRAVITY = 0.25; // Reduced from 0.4 for slower falling
export const JUMP_FORCE = 6; // Reduced from 8 for more controlled jumps

// Pipe constants
export const PIPE_WIDTH = 80;
export const PIPE_GAP = 180; // Increased from 160 for easier gameplay
export const PIPE_SPEED = 2; // Reduced from 3 for slower movement